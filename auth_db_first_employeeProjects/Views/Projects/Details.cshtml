@model auth_db_first_employeeProjects.ViewModels.Projects.ProjectDetailsViewModel

<div>
    <h4>Project Details</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">ID</dt>
        <dd class="col-sm-10">@Model.ProjectId</dd>

        <dt class="col-sm-2">Name</dt>
        <dd class="col-sm-10">@Model.ProjectName</dd>

        <dt class="col-sm-2">Timeline</dt>
        <dd class="col-sm-10">@Model.StartDate.ToShortDateString() - @Model.EndDate.ToShortDateString()</dd>

        @* <dt class="col-sm-2">Creator</dt> *@
        @* <dd class="col-sm-10">@Model.CreatedByNavigation.UserName</dd> *@
    </dl>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>